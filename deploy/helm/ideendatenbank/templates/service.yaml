apiVersion: v1
kind: Service
metadata:
  name: {{ include "ideendatenbank.fullname" . }}
  labels: {{ include "ideendatenbank.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector: {{ include "ideendatenbank.selectorLabels" . | nindent 4 }}
  ports:
  - name: http
    port: {{ .Values.service.port }}
    targetPort: http
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "ideendatenbank.fullname" . }}-headless
  labels: {{ include "ideendatenbank.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  clusterIP: None
  publishNotReadyAddresses: true
  selector: {{ include "ideendatenbank.selectorLabels" . | nindent 4 }}
  ports:
  - name: http
    port: {{ .Values.service.port }}
